{
  "name": "merkle-map-demo",
  "version": "1.2",
  "author": "DFST",
  "license": "Apache-2.0",
  "exports": {
    "types": "./dist/index.d.ts",
    "node": {
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "compile": "rm -rf dist && tsc",
    "build": "rm -rf dist && tsc && rm merkle-map-demo.zip && 7z a -tzip -r -stl -m0=Copy merkle-map-demo.zip dist package.json node_modules",
    "test": "jest tests/*.test.ts",
    "jest": "jest",
    "coverage": "jest --coverage tests/*.test.ts",
    "berkeley": "jest tests/berkeley/multiple-choice.test.ts",
    "deploy": "jest tests/berkeley/deploy.mcq.contract.test.ts",
    "reset": "jest tests/berkeley/reset.mcq.test.ts",
    "baseapi": "jest tests/api/api.base.test.ts",
    "basereset": "jest tests/api/reset.base.test.ts",
    "gc": "node --expose-gc node_modules/jest/bin/jest.js tests/base.test"
  },
  "dependencies": {
    "axios": "^1.6.7",
    "minanft": "^1.6.5",
    "o1js": "^0.16.2",
    "tslib": "^2.6.2",
    "zkcloudworker": "^0.1.4"
  },
  "devDependencies": {
    "@jest/globals": "^29.7.0",
    "@types/jest": "^29.5.12",
    "@types/node": "^20.11.20",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2",
    "typescript": "^5.3.3"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "slowTestThreshold": 1500,
    "testTimeout": 10800000,
    "verbose": true,
    "roots": [
      "tests"
    ],
    "setupFilesAfterEnv": [
      "./jest-config.ts"
    ]
  },
  "packageManager": "yarn@4.1.0"
}
